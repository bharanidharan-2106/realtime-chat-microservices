apiVersion: v1
kind: Service
metadata:
  name: message-service-svc
  namespace: chat-platform
spec:
  type: ClusterIP
  selector:
    app: message-service
  ports:
    - port: 3000
      targetPort: 3000
